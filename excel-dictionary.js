(()=>{"use strict";const data=Array.isArray(window.AVP_EXCEL_DICTIONARY)?window.AVP_EXCEL_DICTIONARY:[],q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)],search=q("#dictSearch"),results=q("#dictResults"),empty=q("#dictEmpty"),count=q("#dictResultCount"),title=q("#dictResultTitle"),level=q("#dictLevel"),detail=q("#dictDetail"),body=q("#dictDetailBody");let activeType="all";const norm=s=>String(s??"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/đ/g,"d").replace(/[^a-z0-9#.+]+/g," ").trim(),esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m])),corpus=e=>norm([e.title,e.subtitle,e.type,e.purpose,e.syntax,(e.keywords||[]).join(" "),(e.causes||[]).join(" "),(e.checks||[]).join(" "),(e.fixes||[]).join(" "),(e.related||[]).join(" ")].join(" "));function score(e,s){if(!s)return 1;const n=norm(s),w=n.split(/\s+/).filter(Boolean),c=corpus(e),t=norm(e.title);let x=0;if(t===n)x+=100;if(t.startsWith(n))x+=55;if(t.includes(n))x+=38;if(c.includes(n))x+=25;for(const k of w){if(t.includes(k))x+=12;else if(c.includes(k))x+=5;else return-1}return x}function filtered(){const s=search.value;return data.map(e=>[e,score(e,s)]).filter(([e,x])=>x>=0&&(activeType==="all"||e.type===activeType)&&(level.value==="all"||e.level===level.value)).sort((a,b)=>b[1]-a[1]||a[0].title.localeCompare(b[0].title,"vi")).map(x=>x[0])}function render(){const a=filtered();count.textContent=`${a.length} kết quả`;title.textContent=activeType==="all"?(search.value.trim()?"Kết quả tìm kiếm":"Tất cả mục"):activeType;results.innerHTML=a.slice(0,180).map(e=>`<article class="dict-card" data-id="${esc(e.id)}" tabindex="0"><div class="dict-card-top"><span class="dict-type">${esc(e.type)}</span><span class="dict-level">${esc(e.level)}</span></div><h3>${esc(e.title)}</h3><div class="sub">${esc(e.subtitle)}</div><p>${esc(e.purpose)}</p>${e.syntax?`<div class="dict-syntax-preview">${esc(e.syntax)}</div>`:""}</article>`).join("");empty.hidden=a.length>0;qa(".dict-card").forEach(c=>{const o=()=>show(data.find(x=>x.id===c.dataset.id));c.onclick=o;c.onkeydown=e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();o()}}})}const section=(n,h)=>h?`<section class="detail-section"><h4>${n}</h4>${h}</section>`:"",list=a=>a?.length?`<ul class="detail-list">${a.map(x=>`<li>${esc(x)}</li>`).join("")}</ul>`:"";function show(e){if(!e)return;const parts=e.parts?.length?`<div class="detail-grid">${e.parts.map(([a,b])=>`<div class="detail-box"><strong>${esc(a)}</strong><span>${esc(b)}</span></div>`).join("")}</div>`:"",rel=e.related?.length?`<div class="detail-related">${e.related.map(r=>`<button type="button" data-rel="${esc(r)}">${esc(r)}</button>`).join("")}</div>`:"";body.innerHTML=`<div class="detail-kicker"><span>${esc(e.type)}</span><span>${esc(e.level)}</span></div><h2 id="dictDetailTitle">${esc(e.title)}</h2><div class="detail-sub">${esc(e.subtitle)}</div><p class="detail-lead">${esc(e.purpose)}</p>${section("Cú pháp / vị trí",e.syntax?`<div class="detail-code">${esc(e.syntax)}</div>`:(e.location?`<div class="detail-code">${esc(e.location)}</div>`:""))}${section("Giải thích từng phần",parts)}${section("Ví dụ cơ bản",e.example?`<div class="detail-code">${esc(e.example)}</div>`:"")}${section("Ví dụ nâng cao / hàm lồng",e.advanced?`<div class="detail-code">${esc(e.advanced)}</div>`:"")}${section("Nguyên nhân thường gặp",list(e.causes))}${section("Kiểm tra ở đâu / kiểm tra gì",list(e.checks))}${section("Cách xử lý",list(e.fixes))}${section("Nội dung liên quan",rel)}${e.note?`<div class="detail-note"><b>Lưu ý:</b> ${esc(e.note)}</div>`:""}`;detail.hidden=false;document.body.style.overflow="hidden";body.querySelectorAll("[data-rel]").forEach(b=>b.onclick=()=>{const s=b.dataset.rel;close();search.value=s;activeType="all";qa(".dict-filter").forEach(x=>x.classList.toggle("is-active",x.dataset.type==="all"));render();search.focus()})}function close(){detail.hidden=true;document.body.style.overflow=""}q("#dictDetailClose").onclick=close;detail.onclick=e=>{if(e.target===detail)close()};document.addEventListener("keydown",e=>{if(e.key==="Escape"&&!detail.hidden)close();if(e.key==="/"&&document.activeElement!==search){e.preventDefault();search.focus()}});const params=new URLSearchParams(location.search),initialQ=params.get("q");if(initialQ)search.value=initialQ;const doctorSearch=document.getElementById("dictDoctorSearch");if(doctorSearch)doctorSearch.onclick=()=>{search.value="lỗi công thức dữ liệu";activeType="Khắc phục";qa(".dict-filter").forEach(x=>x.classList.toggle("is-active",x.dataset.type==="Khắc phục"));render();search.focus();search.scrollIntoView({behavior:"smooth",block:"center"})};search.oninput=render;level.onchange=render;qa(".dict-filter").forEach(b=>b.onclick=()=>{activeType=b.dataset.type;qa(".dict-filter").forEach(x=>x.classList.toggle("is-active",x===b));render()});const counts={};for(const e of data)counts[e.type]=(counts[e.type]||0)+1;q("#count-all").textContent=data.length;for(const[k,v]of Object.entries(counts)){const el=document.getElementById("count-"+k);if(el)el.textContent=v}q("#dictStats").textContent=`${data.length} mục tra cứu · ${counts["Công thức"]||0} hàm · ${counts["Công thức nâng cao"]||0} công thức nâng cao · ${counts["Công cụ"]||0} công cụ · ${counts["Lỗi"]||0} lỗi · ${counts["Khắc phục"]||0} tình huống · ${counts["Tip & Trick"]||0} mẹo`;render()})();